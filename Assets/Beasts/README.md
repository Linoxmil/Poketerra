Sprite files go here, one PNG per species.

Filename must exactly match the `name` field of the corresponding entry in
`../Data/BeastDB.json` — e.g. `Emberkit.png`.

32x32 per frame, animation frames stacked vertically, transparent background.
